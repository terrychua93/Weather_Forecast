import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weather_forecast_reactjs',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
